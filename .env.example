# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=

# Site
NEXT_PUBLIC_SITE_URL=

# Sanity read token (server-side only — never NEXT_PUBLIC_)
SANITY_API_TOKEN=

# Contact form recipient (server-side only — never NEXT_PUBLIC_)
DANIEL_CONTACT_EMAIL=

# SMTP transport (to be confirmed in Sprint 2)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
